###### use requirements without version fixed specifier where possible ######
numpy>=1.16.2
scipy>=1.2.1
scikit-learn
docker
# for database interfacing
pymongo
# for design of experiments
pyDOE
# for Gaussian Processes
gpy
# for sensitivity analysis
SALib>=1.3.3
# for Gaussian processes and BO we need GPFlow, currently we rely on v0.4
# install TensorFlow v1.5 because newer versions will cause weird crashes
tensorflow==1.5
git+git://github.com/GPflow/GPflow.git@0.4.0#egg=gpflow
git+git://github.com/GPflow/GPflowOpt.git@8a2671d8a3ed6825ed5b28fcad86b2ee1e30b467#egg=gpflowopt
# for documentation
sphinx
nbsphinx
jupyter
# making fancy plots
matplotlib
seaborn
plotly
# testing framework
pytest
pytest-mock
# create mock objects for unit testing
mock
# for code coverage purposes
codecov
coverage
# make ascii-art prints
pyfiglet #check if that if enough, original command: conda install -c conda-forge pyfiglet
